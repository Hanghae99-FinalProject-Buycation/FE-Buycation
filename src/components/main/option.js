export const category = () => {
  const categoryList = ["카테고리", "전체", "물건", "음식"];

  return categoryList;
};

export const sort = () => {
  const sortList = ["정렬", "최신 순", "기한 순", "인원 순", "금액 순"];

  return sortList;
};
